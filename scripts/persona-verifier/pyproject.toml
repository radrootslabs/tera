[project]
name = "radroots-persona-verifier"
version = "0.0.0"
requires-python = "==3.14.7"
dependencies = ["jsonschema==4.26.0"]

[tool.uv]
package = false

[dependency-groups]
dev = [
    "ruff==0.12.12",
]
